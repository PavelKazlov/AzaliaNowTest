import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  checkBoxContainer: {
    // padding: 0,
    // marginTop: 2,
    // marginLeft: 0,
    // marginRight: 0,
  },
  wrapperStyle: {
    height: 18,
    width: 18,
    borderWidth: 1,
    borderRadius: 2,
    alignItems: 'center',
    justifyContent: 'center',
  },
})
